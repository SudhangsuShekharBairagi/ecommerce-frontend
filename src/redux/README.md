Redux Toolkit slices for e-commerce state management.

- store.js: root Redux store configuration.
- productSlice.js: products, product details, search, create/update/delete, checkout.
- authSlice.js: login and token persistence.
- profileSlice.js: fetch user profile.
- cartSlice.js: cart item list and persistence.
